#docker-compose up
version: "3.9"
services:
  postgres:
    container_name: localPostgresHomeTask07
    image: postgres
    environment:
      POSTGRES_DB: "local_db"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "12345"
    ports:
      - "5435:5432"   # // 5435:5432 по умолчанию (в апликейшин пропертис тоже меняно)

